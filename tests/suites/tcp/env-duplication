#!/bin/bash

# Simulate 50% packet duplication.
#
# NOTE: netem "duplicate" cannot coexist with other netems in the same HTB
# tree, so this suite runs alone in Round 2 of test-run-all.
NETEM_PORT=8003
NETEM_CLASSID="1:2"
NETEM_QDISC="duplicate 50%"

source "$(dirname $0)/env-base"
